name: "${project.name}"
main: "${project.mainClass}"
version: "${project.version}"
description: "${project.description}"
author: "${project.author}"
softdepend: ["Vault", "LegendChat"]
commands:
  money:
    description: "Para ver o saldo atual"
    aliases: [coin, coins, solaryeconomy]
